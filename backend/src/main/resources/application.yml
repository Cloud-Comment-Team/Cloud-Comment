spring:
  application:
    name: cloud-comment
  profiles:
    default: local
  flyway:
    enabled: true
    locations: classpath:db/migration
  web:
    resources:
      add-mappings: false

server:
  port: ${SERVER_PORT:8080}
